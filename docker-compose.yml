version: "3"
services:
  web:
    build: .
    ports:
      - 80:80
    volumes:
      - ./biosecurity_management:/app/biosecurity_management
    env_file:
      - development.env
    networks:
      - net
networks:
  net: